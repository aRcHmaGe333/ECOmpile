# V1 Public Release (Corrected for V2)

Source: `repo/docs/public_release.md`

## Corrected Direction

ECOmpile V2 focuses on convergent validated outcomes from real usage and compiles them into deterministic execution paths.

Primary message:

- known tasks should execute, not be re-derived
- convergence decides promotion eligibility
- HITL review decides activation

## Public Claims Boundaries

Public-safe V2 claims are grounded in generated artifacts and explicit contracts:

- schema-validated pipeline stages
- deterministic scoring formula and thresholds
- explicit routing hit/fallback behavior
- review-gated activation

## Provider Value

- lower repeated interaction cost on known intents
- more consistent outputs through active kernels
- transparent, inspectable emitted templates
- controllable public/internal artifact boundaries

## What Changed from V1 Narrative

1. Reduced broad conceptual framing and removed noise-heavy framing.
2. Shifted emphasis to deterministic operational pipeline.
3. Enforced public-safe default and redaction boundary by design.
4. Removed dependency assumptions on source-material folders.
